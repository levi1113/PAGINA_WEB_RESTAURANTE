// backend/routes/admin.routes.js

import express from 'express';
const router = express.Router();

// (Aquí pondremos las rutas de admin más adelante)

// ¡Esta es la línea que arregla el error!
export default router;