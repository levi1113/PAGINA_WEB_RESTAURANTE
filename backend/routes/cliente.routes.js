// backend/routes/cliente.routes.js

import express from 'express';
const router = express.Router();

// (Aquí pondremos las rutas de cliente más adelante)

// ¡Esta es la línea que arregla el error!
export default router;